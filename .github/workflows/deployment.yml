name: Deployment

on:
  push:
    branches:
      - main
      - development

jobs:
  deployment:
    name: Deployment
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
        with:
          submodules: true

      - uses: Roblox/setup-foreman@v1
        with:
          version: "^1.0.0"
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Report tool versions
        run: |
          rojo --version
          remodel --version

      - name: Deploy
        run: |
          rojo build --output project.rbxl .
          rojo build --output assets.rbxl assets
          remodel run .remodel/main.lua ${GITHUB_REF#refs/heads/} ${GITHUB_SHA} 7463431274 project.rbxl assets.rbxl --auth "$ROBLOSECURITY"
        env:
          ROBLOSECURITY: ${{ secrets.ROBLOSECURITY }}